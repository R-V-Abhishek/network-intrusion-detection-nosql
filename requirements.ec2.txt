# EC2-only requirements (Option A — Hybrid REST Sync)
# EC2 receives alerts via /api/ingest. It never talks to Cassandra.
# Used by Dockerfile.ec2 multi-stage build.
flask>=3.0.0
redis==5.0.8
requests>=2.31.0
gunicorn>=22.0.0
