services:

  redis-ci:
    image: redis:7.2-alpine
    ports:
      - "6380:6379"   # 👈 ADD THIS LINE
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 10